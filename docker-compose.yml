version: '3'

services:
  # Serviço do aplicativo Laravel
  app:
    image: bitnami/laravel:latest
    container_name: avalia
    volumes:
      - .:/app
    networks:
      - laravel-net
    ports:
      - "1080:8000"

networks:
  laravel-net:
    driver: bridge
