version: '3'

services:
  # Serviço do aplicativo Laravel
  app:
    image: bitnami/laravel:latest
    container_name: avalia
    volumes:
      - .:/app
    networks:
      - laravel-net

networks:
  laravel-net:
    driver: bridge

